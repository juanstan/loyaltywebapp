import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.loyalty.pwa',
  appName: 'myLoyalty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
